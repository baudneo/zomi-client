client:
#  THESE_WILL_BE_IMPORTED: "to the config file to be used as variables"
#  STORE: "sensitive data in this file"
  #######################################################################
  ZM_API: ${ML_INSTALL_CLIENT_ZM_API}
  ZM_PORTAL: ${ML_INSTALL_CLIENT_ZM_PORTAL}
  ZM_USER: ${ML_INSTALL_CLIENT_ZM_USER}
  ZM_PASS: ${ML_INSTALL_CLIENT_ZM_PASS}

  ROUTE_HOST: ${ML_INSTALL_CLIENT_ROUTE_HOST}
  # - Make sure to quote the port number to force it to be a string
  # - leaving the number unquoted will raise a validation error
  ROUTE_PORT: "${ML_INSTALL_CLIENT_ROUTE_PORT}"
  ROUTE_USER: ${ML_INSTALL_CLIENT_ROUTE_USER}
  ROUTE_PASS: ${ML_INSTALL_CLIENT_ROUTE_PASS}

  GOTI_HOST:
  GOTI_PORTAL:
  GOTI_TOKEN:

  PUSHOVER_TOKEN:
  PUSHOVER_KEY:

  LINKURL_USER:
  LINKURL_PASS:

  MQTT_BROKER: 127.0.0.1
  MQTT_USERNAME:
  MQTT_PASSWORD:
